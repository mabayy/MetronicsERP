@{
    ViewData["Title"] = "Akses Ditolak";
    Layout = "_AuthLayout";
}
<div class="auth-card text-center">
    <i class="bi bi-shield-lock text-danger" style="font-size:3rem;"></i>
    <h3 class="fw-bold mt-3">Akses Ditolak</h3>
    <p class="text-muted">Anda tidak memiliki izin untuk mengakses halaman ini.</p>
    <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary">Kembali ke Dashboard</a>
</div>
